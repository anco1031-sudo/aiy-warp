# OS
.DS_Store
Thumbs.db

# Secrets — NEVER commit
.env
*.env
.env.*
*.pem
*.key
*_token*
*token*.json

# Runtime
node_modules/
__pycache__/
*.pyc
.tmp/

# aiy warp — host-local state & config (CLI_SPEC §8.5)
warp.lock
warp.config
*.local.yaml

# aiy warp — migrate originals (DESIGN-NOTE-P1 §5: agents/*.md.bak)
*.bak

# aiy warp — compiled CLI binary (go build -o aiy-warp .)
install/src/aiy-warp
